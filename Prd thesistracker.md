# وثيقة متطلبات المنتج (PRD)
## تطبيق: **مسجّل أفكار الأطروحة** — *Thesis Idea Tracker*

---

> **الإصدار:** v1.0  
> **التاريخ:** أبريل 2026  
> **الحالة:** مسودة أولية للمراجعة  

---

## 1. نظرة عامة (Overview)

### 1.1 ملخص المشكلة

أثناء العمل على الأطروحة الدكتورية، تظهر أفكار مفاجئة مرتبطة بفصول ومباحث ومطالب وعناصر محددة — أفكار تحتاج إلى مراجعة مستقبلية، أو إضافات، أو استخدام في مقالات بحثية — غير أنها تُنسى بسبب انعدام وسيلة تسجيل سريعة ومنظّمة تعكس البنية الهرمية الفعلية للأطروحة.

### 1.2 الحل

تطبيق ويب خفيف من **صفحة واحدة (SPA)** يُرفع على الاستضافة الشخصية، يُمكّن الباحث من تسجيل أفكاره بدقة في مكانها الصحيح ضمن هيكل الأطروحة، واسترجاعها متى احتاج.

### 1.3 المستخدم المستهدف

طالب دكتوراه وحيد يستخدم التطبيق لنفسه (مستخدم واحد، بيئة خاصة).

---

## 2. البيئة التقنية

### 2.1 بيئة الاستضافة

| المورد | التفصيل |
|---|---|
| مزود الاستضافة | DZSecurity — استضافة مشتركة (Starter) |
| نظام التحكم | cPanel |
| قواعد البيانات المدعومة | MariaDB / PostgreSQL |
| لغات الخادم المدعومة | PHP v4.4 → 8.4 |
| التخزين | 100 GB NVMe SSD |
| النسخ الاحتياطي | يومي تلقائي CDP (54 نسخة قابلة الاسترجاع) |

### 2.2 المكدس التقني المختار

| الطبقة | التقنية | السبب |
|---|---|---|
| الواجهة الأمامية | HTML5 + CSS3 + JavaScript (Vanilla) | بدون أي framework — خفيف وبسيط |
| الخادم الخلفي | PHP 8.x | مدعوم بالكامل على cPanel |
| قاعدة البيانات | MariaDB | أبسط إعداد عبر cPanel، أداء ممتاز |
| التواصل | REST API بسيط عبر `fetch()` | JSON بين الواجهة والـ PHP |
| المصادقة | كلمة مرور واحدة (PHP Session) | تطبيق شخصي، لا حاجة لتعقيد |

### 2.3 هيكل الملفات (ملف واحد تقريباً)

```
thesis-tracker/
├── index.html          ← كل واجهة التطبيق (HTML + CSS + JS)
├── api.php             ← كل منطق الخادم وعمليات قاعدة البيانات
├── config.php          ← إعدادات قاعدة البيانات وكلمة المرور
└── install.php         ← سكريبت تهيئة قاعدة البيانات (يُحذف بعد الاستخدام)
```

> ⚠️ **قاعدة صارمة:** لا يُضاف ملف جديد إلا عند ضرورة قصوى. كل المنطق في `api.php` وكل الواجهة في `index.html`.

---

## 3. هيكل البيانات

### 3.1 هيكل الأطروحة (Thesis Structure)

```
الأطروحة
└── فصل (Chapter)
    └── مبحث (Section)
        └── مطلب (Subsection)
            └── عنصر (Element)
```

كل مستوى له: `id` + `title` + `parent_id` + `level` + `order_index`

### 3.2 مخطط قاعدة البيانات (Database Schema)

```sql
-- جدول هيكل الأطروحة
CREATE TABLE thesis_nodes (
    id          INT AUTO_INCREMENT PRIMARY KEY,
    parent_id   INT NULL,                          -- NULL = جذر (فصل)
    level       TINYINT NOT NULL,                  -- 1=فصل, 2=مبحث, 3=مطلب, 4=عنصر
    title       VARCHAR(500) NOT NULL,
    order_index INT DEFAULT 0,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (parent_id) REFERENCES thesis_nodes(id) ON DELETE CASCADE
);

-- جدول الأفكار والملاحظات
CREATE TABLE ideas (
    id          INT AUTO_INCREMENT PRIMARY KEY,
    node_id     INT NOT NULL,                      -- الفصل/المبحث/المطلب/العنصر المرتبط
    idea_type   ENUM(
                    'addition',     -- إضافة مطلوبة
                    'review',       -- مراجعة مستقبلية
                    'article',      -- فكرة لمقال مستقبلي
                    'reference',    -- مرجع يجب إضافته
                    'note'          -- ملاحظة عامة
                ) NOT NULL DEFAULT 'note',
    content     TEXT NOT NULL,
    priority    TINYINT DEFAULT 2,                 -- 1=عاجل, 2=عادي, 3=لاحقاً
    is_done     BOOLEAN DEFAULT FALSE,
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (node_id) REFERENCES thesis_nodes(id) ON DELETE CASCADE
);
```

---

## 4. متطلبات الوظائف (Functional Requirements)

### 4.1 المصادقة (Authentication)

| # | المتطلب |
|---|---|
| A-1 | عند فتح التطبيق يظهر حقل كلمة مرور إذا لم تكن الجلسة نشطة |
| A-2 | كلمة المرور مخزنة كـ hash في `config.php` |
| A-3 | مدة الجلسة: 8 ساعات (قابلة للتعديل) |
| A-4 | زر "تسجيل خروج" في شريط التنقل العلوي |

### 4.2 إدارة هيكل الأطروحة (Thesis Structure Management)

| # | المتطلب |
|---|---|
| S-1 | عرض هيكل الأطروحة كشجرة قابلة للطي/التوسيع في الشريط الجانبي الأيسر |
| S-2 | إضافة فصل / مبحث / مطلب / عنصر بالنقر على زر "+" بجانب أي عنصر |
| S-3 | تعديل عنوان أي عنصر بالنقر المزدوج عليه |
| S-4 | حذف عنصر (مع تحذير بأن كل أفكاره ستُحذف) |
| S-5 | إعادة ترتيب العناصر (drag & drop بسيط أو أزرار ↑↓) |
| S-6 | عرض عدد الأفكار غير المكتملة بجانب كل عنصر كـ badge |

### 4.3 إدارة الأفكار والملاحظات (Ideas Management)

| # | المتطلب |
|---|---|
| I-1 | عند النقر على أي عنصر في الشجرة، يظهر محتواه في اللوح الأيمن |
| I-2 | إضافة فكرة جديدة: محرر نص بسيط + اختيار النوع + الأولوية |
| I-3 | تعديل فكرة موجودة inline (النقر على النص يُحوّله لحقل تعديل) |
| I-4 | حذف فكرة مع تأكيد |
| I-5 | تغيير حالة الفكرة: غير مكتملة ↔ مكتملة (checkbox) |
| I-6 | تصفية الأفكار في اللوح بحسب النوع أو الأولوية |
| I-7 | عند عرض فصل، تظهر كل أفكاره + أفكار مباحثه ومطالبه وعناصره مجمّعة (عرض مجمّع) |

### 4.4 البحث (Search)

| # | المتطلب |
|---|---|
| SR-1 | حقل بحث عالمي في الشريط العلوي |
| SR-2 | البحث في محتوى الأفكار وعناوين العقد |
| SR-3 | النتائج تُظهر: [مسار العقدة] + مقتطف الفكرة |
| SR-4 | النقر على نتيجة ينقل مباشرة لموضعها في الشجرة |

### 4.5 لوحة الملخص (Dashboard)

| # | المتطلب |
|---|---|
| D-1 | الصفحة الرئيسية عند فتح التطبيق تعرض ملخصاً: عدد الأفكار حسب النوع، العاجلة منها، الأخيرة تسجيلاً |
| D-2 | قائمة "آخر 5 أفكار مضافة" مع رابط للذهاب لموضعها |
| D-3 | قائمة "الأفكار العاجلة (الأولوية 1)" |

---

## 5. متطلبات الواجهة (UI Requirements)

### 5.1 التصميم العام

- **اتجاه الكتابة:** RTL (من اليمين لليسار)
- **اللغة الافتراضية:** العربية
- **تخطيط الصفحة:**
  - شريط علوي: الاسم + بحث + تسجيل خروج
  - شريط جانبي أيسر (بمنظور RTL = يمين): شجرة الأطروحة
  - المنطقة الرئيسية: محتوى العقدة المحددة أو الـ Dashboard

```
┌─────────────────────────────────────────────────┐
│  🎓 مسجّل الأطروحة    [بحث...]     [خروج]       │
├──────────────────┬──────────────────────────────┤
│                  │                              │
│  🌳 الهيكل       │   محتوى العقدة / Dashboard  │
│                  │                              │
│  ▼ الفصل الأول   │   ┌─ الفصل الأول ───────┐  │
│    ▶ المبحث 1    │   │ + إضافة فكرة جديدة  │  │
│    ▼ المبحث 2    │   │                      │  │
│      المطلب 1    │   │ [✓] فكرة مكتملة      │  │
│      المطلب 2    │   │ [ ] مراجعة مطلوبة    │  │
│  ▶ الفصل الثاني  │   │ [ ] مقال مستقبلي     │  │
│                  │   └──────────────────────┘  │
└──────────────────┴──────────────────────────────┘
```

### 5.2 ألوان أنواع الأفكار

| النوع | اللون |
|---|---|
| إضافة مطلوبة | 🔵 أزرق |
| مراجعة مستقبلية | 🟠 برتقالي |
| فكرة لمقال | 🟣 بنفسجي |
| مرجع | 🟢 أخضر |
| ملاحظة عامة | ⚪ رمادي |

### 5.3 أولويات الأفكار

| الأولوية | الرمز | المعنى |
|---|---|---|
| 1 | 🔴 | عاجل |
| 2 | 🟡 | عادي |
| 3 | ⚪ | لاحقاً |

### 5.4 الاستجابة (Responsiveness)

- يعمل على شاشات سطح المكتب بالكامل
- على الهاتف: الشريط الجانبي يتحوّل لـ Drawer قابل للإخفاء

---

## 6. API Endpoints (api.php)

جميع الطلبات تمر عبر `api.php?action=XXX` بطريقة POST أو GET.

| Action | Method | الوصف |
|---|---|---|
| `login` | POST | التحقق من كلمة المرور |
| `logout` | POST | إنهاء الجلسة |
| `get_tree` | GET | جلب كامل شجرة الأطروحة |
| `add_node` | POST | إضافة فصل/مبحث/مطلب/عنصر |
| `edit_node` | POST | تعديل عنوان عقدة |
| `delete_node` | POST | حذف عقدة وكل محتوياتها |
| `reorder_node` | POST | تغيير ترتيب عقدة |
| `get_ideas` | GET | جلب أفكار عقدة (مع خيار تجميع الأبناء) |
| `add_idea` | POST | إضافة فكرة جديدة |
| `edit_idea` | POST | تعديل فكرة |
| `delete_idea` | POST | حذف فكرة |
| `toggle_idea` | POST | تغيير حالة إنجاز الفكرة |
| `search` | GET | البحث الشامل |
| `dashboard` | GET | بيانات الصفحة الرئيسية |

---

## 7. الأمان (Security)

| الإجراء | التنفيذ |
|---|---|
| حماية الوصول | PHP Session + كلمة مرور مُشفّرة بـ `password_hash()` |
| منع SQL Injection | Prepared Statements في كل استعلامات MariaDB |
| منع XSS | `htmlspecialchars()` على كل المخرجات |
| حماية ملف الإعداد | `.htaccess` يمنع الوصول لـ `config.php` مباشرة |
| CSRF | Token بسيط في الجلسة للطلبات التعديلية |

---

## 8. خارج النطاق (Out of Scope — v1.0)

- ❌ تعدد المستخدمين
- ❌ مشاركة الأطروحة مع آخرين
- ❌ تحميل مرفقات أو صور
- ❌ تزامن مع تطبيقات خارجية
- ❌ نظام تنبيهات/تذكيرات

---

## 9. اقتراحات تطوير مستقبلية (v1.x)

هذه أفكار عملية وخفيفة لا تُعقّد المشروع:

### 9.1 تصدير البيانات
- زر "تصدير كـ Markdown" يُنتج ملف `.md` بكامل هيكل الأطروحة وأفكارها — مفيد للنسخ الاحتياطي اليدوي أو الطباعة.

### 9.2 وضع "التركيز"
- عند العمل على فصل معين، زر يُخفي كل شيء ويعرض فقط أفكار هذا الفصل بشاشة كاملة.

### 9.3 إحصائيات بسيطة
- صفحة تعرض: عدد الأفكار لكل فصل كـ bar chart بسيط بـ CSS فقط (بدون مكتبات).

### 9.4 وسوم (Tags)
- إضافة حقل tags اختياري لكل فكرة (مثل: `#منهجية`, `#مصادر`) مع فلترة سريعة.

### 9.5 رابط سريع
- كل فكرة لها رابط permalink مثل `index.html#idea-42` للرجوع إليها مباشرة.

### 9.6 وضع "القراءة السريعة"
- عرض كل أفكار النوع "مراجعة" من كامل الأطروحة في صفحة واحدة مرتبة حسب الفصول.

---

## 10. معايير القبول (Acceptance Criteria)

| المعيار | المتطلب |
|---|---|
| الأداء | الصفحة تُحمّل كاملة في < 2 ثانية |
| الموثوقية | البيانات تُحفظ فوراً بعد كل تعديل |
| الاستخدام | إضافة فكرة جديدة لا تحتاج أكثر من 3 نقرات |
| التنقل | لا يحدث تحديث للصفحة (full page reload) في أي عملية |
| الأمان | لا يمكن الوصول لأي بيانات بدون تسجيل دخول |
| التوافق | يعمل على Chrome و Firefox و Safari الحديثة |

---

## 11. خطوات التنفيذ المقترحة

```
المرحلة 1 (الأساس) — يومان:
  ✓ إعداد قاعدة البيانات عبر cPanel
  ✓ كتابة config.php و install.php
  ✓ بناء api.php مع كل الـ endpoints
  ✓ نظام تسجيل الدخول

المرحلة 2 (الهيكل) — يومان:
  ✓ واجهة شجرة الأطروحة (الشريط الجانبي)
  ✓ إضافة / تعديل / حذف العقد
  ✓ حفظ وجلب البيانات

المرحلة 3 (الأفكار) — يومان:
  ✓ لوح الأفكار الرئيسي
  ✓ إضافة / تعديل / حذف / إنجاز الأفكار
  ✓ الفلترة حسب النوع والأولوية

المرحلة 4 (الإضافات) — يوم:
  ✓ البحث الشامل
  ✓ Dashboard / الصفحة الرئيسية
  ✓ التجاوب مع الهاتف
```

---

## 12. ملاحظات للمطوّر

1. **لا تستخدم أي Framework** (لا React، لا Vue، لا Laravel) — Vanilla JS + PHP كافيان تماماً.
2. **الشجرة الجانبية** تُبنى بـ `<ul><li>` متداخلة مع CSS لإظهار المستويات.
3. **كل البيانات** تُجلب أول مرة كـ JSON وتُخزّن في متغير JavaScript، لتحديث الواجهة بدون طلبات زائدة.
4. **MariaDB** تُنشأ عبر cPanel → MySQL Databases بضغطة واحدة.
5. **`config.php`** يجب أن يكون خارج `public_html` إن أمكن، أو محمياً بـ `.htaccess`.

---

---

## 13. مواصفات التصميم — UI/UX Design Specification

> **المرجع البصري:** لوحات تحكم (Dashboard) عصرية ذات بطاقات ملونة، شريط جانبي أنيق، بيانات مرئية، وتجربة مستخدم سلسة — مُكيَّفة لبيئة أكاديمية عربية بالكامل.

---

### 13.1 الهوية البصرية — Design Identity

#### المفهوم الإبداعي
> **"الباحث الهادئ"** — تصميم يُوحي بالعمق الفكري والتنظيم الأكاديمي، دون ثقل أو تعقيد. نظيف كورقة بحثية، ذكي كفهرس موسوعة، سريع كخاطرة علمية.

#### الشخصية البصرية
- **النبرة:** احترافية + دافئة، بلا رسمية مُفرطة
- **الإيقاع:** هادئ ومنظّم — كل عنصر في مكانه
- **الانطباع الأول:** "هذا مكاني أنا"، لا "هذا نظام معقد"

---

### 13.2 لوحة الألوان — Color Palette

```css
:root {
  /* ━━━ اللون الرئيسي — Primary (أزرق لازوردي أكاديمي) ━━━ */
  --primary-50:  #EEF2FF;
  --primary-100: #E0E7FF;
  --primary-400: #818CF8;
  --primary-500: #6366F1;   /* ← اللون الرئيسي */
  --primary-600: #4F46E5;
  --primary-700: #4338CA;

  /* ━━━ الخلفية العامة ━━━ */
  --bg-base:     #F8F9FE;   /* أبيض مائل للبنفسجي الخفيف */
  --bg-card:     #FFFFFF;
  --bg-sidebar:  #FFFFFF;

  /* ━━━ ألوان أنواع الأفكار ━━━ */
  --type-addition:  #6366F1;  /* إضافة   — بنفسجي أزرق */
  --type-review:    #F59E0B;  /* مراجعة  — عنبري دافئ  */
  --type-article:   #8B5CF6;  /* مقال    — بنفسجي     */
  --type-reference: #10B981;  /* مرجع    — أخضر زمردي */
  --type-note:      #6B7280;  /* ملاحظة  — رمادي     */

  /* ━━━ بطاقات الإحصاء (Gradient Cards) ━━━ */
  --grad-chapters:  linear-gradient(135deg, #667EEA 0%, #764BA2 100%);
  --grad-sections:  linear-gradient(135deg, #4FACFE 0%, #00F2FE 100%);
  --grad-ideas:     linear-gradient(135deg, #43E97B 0%, #38F9D7 100%);
  --grad-urgent:    linear-gradient(135deg, #FA709A 0%, #FEE140 100%);

  /* ━━━ الأولويات ━━━ */
  --priority-high:   #EF4444;
  --priority-medium: #F59E0B;
  --priority-low:    #94A3B8;

  /* ━━━ النصوص ━━━ */
  --text-primary:   #1E1B4B;   /* داكن بنفسجي */
  --text-secondary: #6B7280;
  --text-muted:     #9CA3AF;

  /* ━━━ الحدود والظلال ━━━ */
  --border:         #E5E7EB;
  --shadow-card:    0 1px 3px rgba(99,102,241,0.06), 0 4px 16px rgba(99,102,241,0.08);
  --shadow-hover:   0 8px 30px rgba(99,102,241,0.15);
  --shadow-sidebar: 2px 0 20px rgba(0,0,0,0.06);
  --radius-card:    16px;
  --radius-btn:     10px;
  --radius-badge:   20px;
}
```

---

### 13.3 الخطوط — Typography

#### الخطوط المختارة من Google Fonts (عربية)

```html
<!-- في <head> -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">
```

| الدور | الخط | الوزن | الاستخدام |
|---|---|---|---|
| العناوين الرئيسية | **Cairo** | 800 / 700 | اسم التطبيق، عناوين الأقسام |
| العناوين الثانوية | **Cairo** | 600 | عناوين البطاقات، الفصول |
| نص الواجهة | **Tajawal** | 500 | القوائم، الأزرار، التسميات |
| نص المحتوى | **Tajawal** | 400 | محتوى الأفكار، الأوصاف |
| الأرقام والإحصاء | **Cairo** | 800 | أرقام Dashboard |

```css
body {
  font-family: 'Tajawal', sans-serif;
  direction: rtl;
}
h1, h2, h3, .app-title, .stat-number {
  font-family: 'Cairo', sans-serif;
}
```

#### تدرّج أحجام النص

```css
--text-xs:   12px;   /* التسميات الصغيرة، الـ timestamps */
--text-sm:   13px;   /* النص الثانوي، الـ badges */
--text-base: 15px;   /* نص الواجهة الافتراضي */
--text-md:   17px;   /* عناوين القائمة الجانبية */
--text-lg:   20px;   /* عناوين الأقسام */
--text-xl:   24px;   /* عناوين الصفحة */
--text-2xl:  32px;   /* أرقام الإحصاء */
--text-3xl:  40px;   /* الرقم الرئيسي في الـ Dashboard */
```

---

### 13.4 الأيقونات — Icons

#### المكتبة المختارة: **Lucide Icons** + **Phosphor Icons**

```html
<!-- Lucide (خفيف ونظيف) -->
<script src="https://unpkg.com/lucide@latest/dist/umd/lucide.min.js"></script>
```

#### توزيع الأيقونات على العناصر

| العنصر | الأيقونة | الاسم |
|---|---|---|
| التطبيق / الشعار | 🎓 | `graduation-cap` |
| لوحة التحكم | `layout-dashboard` | Dashboard |
| الفصول | `book-open` | Chapters |
| المباحث | `bookmark` | Sections |
| المطالب | `list` | Subsections |
| العناصر | `hash` | Elements |
| إضافة مطلوبة | `plus-circle` | Addition |
| مراجعة | `refresh-cw` | Review |
| مقال مستقبلي | `pen-tool` | Article |
| مرجع | `link` | Reference |
| ملاحظة | `sticky-note` | Note |
| بحث | `search` | Search |
| تسجيل خروج | `log-out` | Logout |
| عاجل | `alert-circle` | Urgent |
| مكتمل | `check-circle-2` | Done |
| حذف | `trash-2` | Delete |
| تعديل | `edit-3` | Edit |
| طيّ / توسيع | `chevron-down` | Expand |
| إضافة عقدة | `folder-plus` | New Node |
| تصدير | `download` | Export |

---

### 13.5 التخطيط الكامل — Full Layout Specification

#### هيكل الصفحة (RTL)

```
┌─────────────────────────────────────────────────────────────────┐
│  TOPBAR                                                         │
│  [🎓 مسجّل الأطروحة]   [🔍 ابحث في أفكارك...]    [خروج 👤]   │
├──────────────────────────┬──────────────────────────────────────┤
│  SIDEBAR (280px)         │  MAIN CONTENT                        │
│  ─────────────────       │  ─────────────────────────────────   │
│  📊 لوحة التحكم          │                                      │
│  ─────────────────       │  ╔══════════════════════════════╗    │
│  🌳 هيكل الأطروحة        │  ║  Dashboard / Node Content    ║    │
│                          │  ║                              ║    │
│  ▼ 📖 الفصل الأول [3]   │  ║  Stat Cards Row              ║    │
│    ▶ المبحث 1.1          │  ║  ┌──────┐┌──────┐┌──────┐   ║    │
│    ▼ المبحث 1.2 [2]      │  ║  │      ││      ││      │   ║    │
│      • المطلب 1.2.1      │  ║  └──────┘└──────┘└──────┘   ║    │
│      • المطلب 1.2.2      │  ║                              ║    │
│  ▶ 📖 الفصل الثاني [1]  │  ║  Recent Ideas + Urgent       ║    │
│  ▶ 📖 الفصل الثالث      │  ╚══════════════════════════════╝    │
│                          │                                      │
│  ─────────────────       │                                      │
│  [+ إضافة فصل جديد]     │                                      │
└──────────────────────────┴──────────────────────────────────────┘
```

#### أبعاد العناصر الأساسية

| العنصر | الارتفاع | العرض | ملاحظة |
|---|---|---|---|
| الشريط العلوي (Topbar) | 64px | 100% | ثابت في الأعلى |
| الشريط الجانبي | 100vh - 64px | 280px | ثابت على اليمين (RTL) |
| بطاقة الإحصاء | 100px | 1fr | تتوزع 4 في صف |
| بطاقة الفكرة | auto min 80px | 100% | تتمدد حسب المحتوى |
| حقل الإدخال السريع | 44px | 100% | في أسفل اللوح |

---

### 13.6 مكونات الواجهة — UI Components

#### أ. الشريط العلوي (Topbar)

```
خلفية: #FFFFFF  |  ظل سفلي خفيف  |  Z-index: 100
├─ [يمين] شعار التطبيق: أيقونة 🎓 + "مسجّل الأطروحة" (Cairo Bold)
├─ [وسط]  حقل بحث منسجم بعرض 380px، حواف دائرية 24px، أيقونة بحث
└─ [يسار] صورة المستخدم الرمزية (Avatar) + زر تسجيل خروج
```

#### ب. الشريط الجانبي (Sidebar)

```
خلفية: #FFFFFF  |  ظل يساري خفيف  |  عرض: 280px
├─ قسم التنقل:
│  ├─ زر "لوحة التحكم" — نشط = خلفية primary-50 + نص primary-600
│  └─ عنوان قسم "🌳 هيكل الأطروحة"
│
├─ شجرة الهيكل (Tree):
│  ├─ فصل: أيقونة 📖 + نص Cairo 600 + badge عدد الأفكار + زر +
│  ├─ مبحث: مسافة 20px + نقطة + نص Tajawal 500
│  ├─ مطلب: مسافة 36px + خط رفيع + نص Tajawal 400
│  └─ عنصر: مسافة 52px + نقطة صغيرة + نص Tajawal 400 رمادي
│
└─ زر "إضافة فصل" في الأسفل — خط منقط + أيقونة +
```

**تصميم عنصر الشجرة:**
- الحالة العادية: خلفية شفافة، نص `--text-primary`
- الـ Hover: خلفية `--primary-50`، نص `--primary-600`، انتقال 150ms
- الحالة المحددة: خلفية `--primary-100`، خط جانبي 3px `--primary-500`، نص `--primary-700`
- الـ Badge: دائري، خلفية `--primary-500`، نص أبيض، 18px × 18px

#### ج. بطاقات الإحصاء (Stat Cards)

أربع بطاقات في الـ Dashboard بتدرجات لونية مختلفة، مستوحاة من الصور المرفقة:

```css
.stat-card {
  border-radius: 20px;
  padding: 20px 24px;
  color: white;
  position: relative;
  overflow: hidden;
}
.stat-card::after {
  /* دائرة زخرفية في الزاوية */
  content: '';
  position: absolute;
  width: 80px; height: 80px;
  border-radius: 50%;
  background: rgba(255,255,255,0.15);
  bottom: -20px; left: -20px;
}
```

| البطاقة | التدرج | الأيقونة | القيمة |
|---|---|---|---|
| إجمالي الفصول | `var(--grad-chapters)` | `book-open` | عدد الفصول |
| إجمالي الأفكار | `var(--grad-sections)` | `lightbulb` | عدد الأفكار |
| أفكار مكتملة | `var(--grad-ideas)` | `check-circle-2` | % الإنجاز |
| أفكار عاجلة | `var(--grad-urgent)` | `alert-circle` | عدد العاجلة |

#### د. بطاقة الفكرة (Idea Card)

```
┌─────────────────────────────────────────────────┐
│  [🔵 إضافة]  [🔴 عاجل]            [✏️] [🗑️]    │
│                                                  │
│  نص الفكرة يظهر هنا بخط Tajawal 15px           │
│  ويمتد لأكثر من سطر إذا كان المحتوى طويلاً     │
│                                                  │
│  📍 الفصل الأول › المبحث الثاني    🕐 منذ يومين │
└─────────────────────────────────────────────────┘
```

```css
.idea-card {
  background: var(--bg-card);
  border-radius: 14px;
  border: 1px solid var(--border);
  padding: 16px 20px;
  box-shadow: var(--shadow-card);
  transition: box-shadow 0.2s ease, transform 0.2s ease;
  border-right: 4px solid var(--type-color); /* لون نوع الفكرة */
}
.idea-card:hover {
  box-shadow: var(--shadow-hover);
  transform: translateY(-2px);
}
.idea-card.done {
  opacity: 0.55;
  border-right-color: #D1D5DB;
}
```

#### هـ. نموذج الإضافة السريعة (Quick Add)

شريط ثابت في أسفل لوح المحتوى:

```
┌─────────────────────────────────────────────────────────────┐
│  [💡 نوع ▾]  [🔴 أولوية ▾]  اكتب فكرتك هنا...   [إضافة →] │
└─────────────────────────────────────────────────────────────┘
```

- خلفية `#FFFFFF`، ظل علوي خفيف
- الحقل: `border-radius: 12px`، `border: 2px solid transparent`
- عند التركيز: `border-color: var(--primary-400)` مع `box-shadow` خفيف
- زر الإضافة: تدرج `--grad-chapters`، `border-radius: 10px`

---

### 13.7 الحركة والتفاعل — Motion & Micro-interactions

#### مبدأ التحريك
> حركة هادفة فقط — لا زخرفة بلا معنى. كل حركة توجّه الانتباه أو تؤكد تصرفاً.

| الحدث | الحركة | المدة |
|---|---|---|
| فتح التطبيق | fade-in + slide-up للبطاقات (staggered 80ms) | 400ms |
| النقر على عقدة | slide-in من اليمين للمحتوى | 250ms |
| إضافة فكرة | fade-in + scale من 0.95 إلى 1 | 200ms |
| حذف فكرة | slide-out + fade + collapse height | 300ms |
| تحديد فكرة كمكتملة | strike-through متحرك + تغير opacity | 400ms |
| hover على البطاقات | translateY(-2px) + shadow أعمق | 200ms |
| hover على الشريط الجانبي | خلفية تتوسع من اليمين | 150ms |
| فتح Dropdown | scale من 0.95 + fade-in | 150ms |
| تحميل البيانات | Skeleton shimmer (موجة لامعة) | متكرر |

```css
/* Skeleton Loading */
.skeleton {
  background: linear-gradient(90deg,
    #F0F0F0 25%, #E8E8E8 50%, #F0F0F0 75%);
  background-size: 200% 100%;
  animation: shimmer 1.4s infinite;
  border-radius: 8px;
}
@keyframes shimmer {
  0%   { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

---

### 13.8 التصميم للهاتف — Mobile Responsive Design

#### نقاط الكسر (Breakpoints)

```css
/* Tablet */
@media (max-width: 1024px) { ... }

/* Mobile */
@media (max-width: 768px) { ... }

/* Small Mobile */
@media (max-width: 480px) { ... }
```

#### التكيّف على الهاتف

```
📱 شاشة الهاتف:
┌───────────────────────────────┐
│ [☰]  🎓 مسجّل الأطروحة  [🔍]│  ← Topbar مضغوط (56px)
├───────────────────────────────┤
│                               │
│  ┌──────┐ ┌──────┐ ┌──────┐  │  ← بطاقات إحصاء قابلة للتمرير
│  │  5   │ │  24  │ │  75% │  │    (Horizontal Scroll)
│  └──────┘ └──────┘ └──────┘  │
│                               │
│  ─── آخر الأفكار ─────────── │
│  ┌───────────────────────────┐│
│  │ 💡 فكرة لمقال مستقبلي    ││
│  │ الفصل الثاني › المبحث 1  ││
│  └───────────────────────────┘│
│                               │
│  + إضافة فكرة جديدة           │
└───────────────────────────────┘

الشريط الجانبي = Bottom Sheet يظهر عند الضغط على [☰]
يتحرك من الأسفل للأعلى بـ slide-up animation
```

#### تعديلات الهاتف

```css
@media (max-width: 768px) {
  /* الشريط الجانبي يصبح Bottom Sheet */
  .sidebar {
    position: fixed;
    bottom: 0; right: 0; left: 0;
    height: 75vh;
    border-radius: 24px 24px 0 0;
    transform: translateY(100%);
    transition: transform 0.35s cubic-bezier(0.32, 0.72, 0, 1);
    z-index: 200;
  }
  .sidebar.open { transform: translateY(0); }

  /* بطاقات الإحصاء: صف أفقي بتمرير */
  .stat-cards-row {
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    gap: 12px;
    padding: 0 16px;
  }
  .stat-card {
    min-width: 160px;
    scroll-snap-align: start;
  }

  /* نموذج الإضافة: زر عائم */
  .quick-add-bar {
    position: fixed;
    bottom: 20px; left: 20px; right: 20px;
    border-radius: 16px;
    box-shadow: 0 8px 32px rgba(99,102,241,0.25);
    z-index: 150;
  }
}
```

#### شريط التنقل السفلي (Mobile Bottom Nav)

```
┌────────────────────────────────────────────────┐
│  🏠 الرئيسية  │  🌳 الهيكل  │  🔍 بحث  │  💡  │
└────────────────────────────────────────────────┘
```
4 تبويبات ثابتة أسفل الشاشة على الهاتف بدل الشريط الجانبي.

---

### 13.9 نظام شاشة تسجيل الدخول — Login Screen

```
┌──────────────────────────────────────────────┐
│           خلفية: تدرج ناعم                   │
│    #EEF2FF → #FFFFFF (زاوية 145deg)          │
│                                              │
│         🎓                                   │
│    مسجّل الأطروحة                            │
│    أطروحتك، مُنظَّمة ومحمية                  │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │  🔒  كلمة المرور                    │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  ┌──────────────────────────────────────┐    │
│  │         دخول إلى مساحتي             │    │
│  └──────────────────────────────────────┘    │
│                                              │
│  الدخول آمن وخاص — هذا التطبيق لك وحدك     │
└──────────────────────────────────────────────┘
```

- البطاقة المركزية: `border-radius: 24px`، ظل `0 20px 60px rgba(99,102,241,0.15)`
- حقل كلمة المرور: عرض 360px، حواف 14px
- الزر: تدرج `--grad-chapters`، عرض كامل، ارتفاع 52px
- عند الخطأ: اهتزاز أفقي `shake animation` 400ms

---

### 13.10 ملخص قرارات التصميم — Design Decisions Summary

| القرار | الاختيار | السبب |
|---|---|---|
| نمط الألوان | أزرق بنفسجي (Indigo) | أكاديمي، راقٍ، غير مبتذل |
| خلفية التطبيق | أبيض مائل `#F8F9FE` | يُبرز البطاقات البيضاء |
| خط العناوين | Cairo | أجمل خط عربي على Google Fonts لعناوين القوائم |
| خط المحتوى | Tajawal | سلاسة في القراءة الطويلة، وزن متوازن |
| الأيقونات | Lucide | خطوط رفيعة + انسجام بصري عالٍ |
| حواف البطاقات | 16px | عصري دون مبالغة |
| الظل | بنفسجي خفيف | يتناسق مع اللون الرئيسي |
| الجانب الثابت | يمين (RTL) | طبيعي لمستخدم عربي |
| الحركة | Ease-out هادئة | مريحة للجلسات الطويلة |
| الهاتف | Bottom Sheet + Bottom Nav | أفضل تجربة باليد الواحدة |

---

*وثيقة PRD — مسجّل أفكار الأطروحة | إصدار 1.1 | أبريل 2026*  
*القسم 13 مُضاف: مواصفات UI/UX الكاملة*
